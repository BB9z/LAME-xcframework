name: CI

on: [push, pull_request]

jobs:
  build:
    runs-on: macos-13
    steps:
    - uses: actions/checkout@v4
    - name: XCFramework Make
      run: |
        cd macosx
        ./make-xcframework.sh
